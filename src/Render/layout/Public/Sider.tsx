import React from 'react';

export interface BaseSiderProps {}

export const Slider: React.FC = ({ children }) => {
  return <section className={`app-layout-sider`}></section>;
};

export default Slider;
